Deno.test("legacy validation loop coverage moved to lifecycle state tests", () => {
    // The hidden multi-phase validation loop was removed. Lifecycle behavior is covered by validation-lifecycle-*.test.js.
});
