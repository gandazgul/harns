{
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "Write|Edit|MultiEdit",
                "hooks": [
                    {
                        "type": "command",
                        "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$f\" in */src/*.ts|*/src/*.tsx|*/src/*.js|*/src/*.jsx) out=$(deno task -q seams:check 2>&1) || { printf '%s\\n' \"$out\"; exit 2; };; esac",
                        "timeout": 30,
                        "statusMessage": "Checking test-injection seams"
                    }
                ]
            }
        ]
    }
}
