# Validation Loop Test Disposition

| Test                                                                                                      | File         | Disposition | Detail                                                                                                                                                                                                                              |
| --------------------------------------------------------------------------------------------------------- | ------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shouldContinueParentEpicAfterValidation ignores standalone FEATURE plans`                                | core         | rewritten   | Seeded at `validated_reviewer`; asserts standalone FEATURE publication verifies through `runValidationLoop` without returning an Epic continuation.                                                                                 |
| `runValidationLoop skips semantic review and merge-back for non-Git in-place execution`                   | core         | removed     | Asserted multiple phases running through one driver call for non-Git validation; the lifecycle dispatcher now advances one persisted Plan Status per call.                                                                          |
| `runValidationLoop starts Manual QA and Work Record generation concurrently after FEATURE validation`     | core         | removed     | Asserted post-verification handoff fan-out after the old driver completed validation in one call; lifecycle validation now reaches publication from `validated_reviewer` only and handoff behavior is owned by publication helpers. |
| `runValidationLoop does not switch active agent unless finalAgentName is provided`                        | core         | removed     | Asserted final-agent restoration in the old multi-phase driver; the lifecycle dispatcher no longer accepts or performs final agent switching.                                                                                       |
| `runValidationLoop marks validation progress and success messages with status styling`                    | core         | removed     | Asserted old driver UI progress aggregation across CI, review, and delivery; lifecycle phases now emit phase-local status events rather than one combined progress record.                                                          |
| `runValidationLoop cancels CI without dispatching repair and leaves Engineer active`                      | core         | removed     | Asserted canceled CI handling through old continuation plumbing; cancellation now returns a paused implemented-phase result without the old active-agent continuation loop.                                                         |
| `runValidationLoop restores requested final agent after validation`                                       | core         | removed     | Asserted `finalAgentName` restoration after the old all-in-one driver; that option and behavior were removed by lifecycle-state dispatch.                                                                                           |
| `runValidationLoop fails FEATURE validation when workflow diff is empty`                                  | core         | rewritten   | Seeded at `validated_ci`; asserts empty workflow diff fails FEATURE validation and records the failed lifecycle transition.                                                                                                         |
| `runValidationLoop fails PROJECT validation when workflow diff only changes a plan document`              | core         | rewritten   | Seeded at `validated_ci`; asserts plan-document-only diff fails PROJECT validation and records the failed lifecycle transition.                                                                                                     |
| `runValidationLoop stages validation_passed before worktree merge succeeds`                               | delivery     | removed     | Asserted worktree merge ordering inside the old driver; worktree publication now records delivery evidence from the `validated_reviewer` phase after merge verification.                                                            |
| `runValidationLoop merges verified Plan metadata in Git and leaves the primary checkout clean`            | delivery     | removed     | Asserted real Git merge-back proof coupled to the deleted multi-phase driver; lifecycle publication delegates this to Direct Delivery transition/proof helpers.                                                                     |
| `runValidationLoop reapplies verified Plan metadata after real merge-conflict rollback`                   | delivery     | removed     | Asserted rollback of primary Plan metadata during old merge failure recovery; lifecycle publication records `worktree_merge_failed` and dispatches merge repair instead of retrying inside one call.                                |
| `runValidationLoop does not preserve a nonexistent Plan path for quick-fix worktrees`                     | delivery     | rewritten   | Seeded at `validated_reviewer`; asserts non-Git QUICK_FIX publication verifies the Plan and records Direct Delivery evidence without worktree Plan preservation.                                                                    |
| `runValidationLoop halts and preserves worktree when post-merge verification fails`                       | delivery     | removed     | Asserted halt behavior after post-merge verification in the old driver; lifecycle publication fails the `validated_reviewer` phase and records merge failure evidence.                                                              |
| `an unresolved journal blocks validation settlement with an actionable message`                           | delivery     | removed     | Asserted settlement-journal blocking in the old merge-back driver; lifecycle transitions now own unsettled journal detection outside validation phase tests.                                                                        |
| `Direct Delivery publishes an Epic child while a sibling is still being planned`                          | delivery     | removed     | Asserted Epic sibling publication policy in the old Direct Delivery driver; lifecycle publication now records delivery evidence per validated child and Epic continuation is handled separately.                                    |
| `Direct Delivery refuses an Epic child when a sibling claims verified without evidence`                   | delivery     | removed     | Asserted sibling evidence validation inside old publication plumbing; lifecycle refactor removed sibling scanning from `runValidationLoop`.                                                                                         |
| `Direct Delivery publishes an Epic child when a verified sibling carries delivery evidence`               | delivery     | removed     | Asserted sibling delivery-evidence acceptance in the old publication path; lifecycle validation no longer decides sibling settlement inside a single run.                                                                           |
| `runValidationLoop runs always human review after semantic approval and before merge`                     | human-review | rewritten   | Seeded at `validated_reviewer`; asserts `always` mode opens code review once, records approval metadata, and pauses before publication.                                                                                             |
| `runValidationLoop ask mode can skip human review and merge`                                              | human-review | rewritten   | Seeded at `validated_reviewer`; asserts `ask` mode persists a skipped decision and pauses before publication.                                                                                                                       |
| `runValidationLoop ask mode opens human review before merge when approved`                                | human-review | rewritten   | Seeded at `validated_reviewer`; asserts `ask` mode opens code review when selected and persists an approved decision.                                                                                                               |
| `runValidationLoop sends human feedback to active execution owner and continues validation`               | human-review | removed     | Asserted feedback repair and same-call continuation in the old driver; lifecycle human review now dispatches feedback repair and returns a paused phase result.                                                                     |
| `runValidationLoop treats human review exit as validation failure without merge`                          | human-review | removed     | Asserted exit handling coupled to merge suppression in the old driver; lifecycle human review records `validation_failed` at `validated_reviewer` without entering publication.                                                     |
| `runValidationLoop keeps reopening code review for as many feedback rounds as the human gives`            | human-review | removed     | Asserted an internal human-review loop; lifecycle refactor removed loops inside one `runValidationLoop` call and requires a new call after repair completion.                                                                       |
| `runValidationLoop ends the human review loop only when the human quits`                                  | human-review | removed     | Asserted old repeated code-review loop termination; lifecycle human review handles one decision per invocation.                                                                                                                     |
| `runValidationLoop resuming mid-human-review does not restart automatic semantic rounds`                  | human-review | removed     | Asserted continuation state spanning semantic and human review inside the old driver; persisted Plan Status now resumes directly at `validated_reviewer`.                                                                           |
| `runValidationLoop restores a real missing worktree Plan and continues validation in the same call`       | recovery     | removed     | Asserted missing worktree Plan restoration plus same-call CI continuation; lifecycle dispatch no longer restores and advances through multiple phases in one invocation.                                                            |
| `runValidationLoop reports restored Plan file once and continues CI without spurious validation_failed`   | recovery     | removed     | Asserted old restoration reporting and immediate CI continuation; lifecycle-state validation removed that recovery loop.                                                                                                            |
| `runValidationLoop keeps merged worktree when cleanup setting is disabled`                                | recovery     | removed     | Asserted cleanup policy after old merge settlement; lifecycle publication settlement owns cleanup independently of validation recovery.                                                                                             |
| `runValidationLoop records worktree_merge_failed when merge-back fails`                                   | recovery     | removed     | Asserted merge-back failure recording from the old recovery driver; lifecycle publication records this from the `validated_reviewer` phase.                                                                                         |
| `runValidationLoop still prompts when merge-conflict metadata updates fail`                               | recovery     | removed     | Asserted merge-conflict prompt fallback in old merge repair plumbing; lifecycle refactor removed prompt-and-retry loops from one validation call.                                                                                   |
| `runValidationLoop recovers missing worktree target branch from registry before merge`                    | recovery     | removed     | Asserted target-branch recovery from registry before merge; lifecycle context resolution now fails closed unless explicit delivery identity is present.                                                                             |
| `runValidationLoop fails closed instead of using guarded primary-checkout fallback`                       | recovery     | rewritten   | Seeded at `implemented`; asserts missing target branch metadata fails before CI and leaves validation counters unchanged.                                                                                                           |
| `runValidationLoop dispatches active owner merge repair and retries merge-back`                           | recovery     | removed     | Asserted merge repair dispatch plus retry in one driver call; lifecycle publication dispatches repair but does not retry until a later invocation.                                                                                  |
| `runValidationLoop completes after merge repair task_completed and retry`                                 | recovery     | removed     | Asserted task_completed-driven merge retry inside old continuation plumbing; lifecycle refactor deleted same-call repair completion handling.                                                                                       |
| `runValidationLoop retries worktree merge after user fixes primary checkout`                              | recovery     | removed     | Asserted user-fixed merge retry loop; lifecycle validation now records failure and requires a later phase invocation.                                                                                                               |
| `runValidationLoop marks active worktree validation_failed when validation fails`                         | recovery     | removed     | Asserted old active-worktree failure marking during multi-phase recovery; lifecycle transitions now persist Plan status events rather than mutate that driver state.                                                                |
| `runValidationLoop mechanically retries when target branch advances before publication`                   | recovery     | removed     | Asserted nested mechanical retry before publication; lifecycle refactor removed nested CI retry loops from publication.                                                                                                             |
| `runValidationLoop pauses with Engineer when CI repair does not call task_completed`                      | repair       | rewritten   | Seeded at `implemented`; asserts failed CI dispatches Engineer repair, preserves validation continuation workflow, and increments `validationCiAttempts`.                                                                           |
| `runValidationLoop pauses when the Reviewer-Feedback Engineer stalls`                                     | repair       | removed     | Asserted semantic repair stall through old injected isolated session and continuation loop; lifecycle semantic repair now returns one phase result and records failure when the boundary does not complete.                         |
| `runValidationLoop preserves Frontend Engineer owner when CI repair pauses`                               | repair       | rewritten   | Seeded at `implemented`; asserts CI repair dispatch uses `frontend-engineer` when the active execution owner is Frontend Engineer.                                                                                                  |
| `runValidationLoop clears transient Frontend Engineer repair context after observed CI repair completion` | repair       | removed     | Asserted transient repair context cleanup after same-call task_completed detection; lifecycle refactor removed that transient completion detector.                                                                                  |
| `runValidationLoop routes frontend semantic repair to the Reviewer-Feedback Engineer`                     | repair       | removed     | Asserted semantic repair routing via old round loop; lifecycle semantic feedback uses the `semanticReviewPort` Reviewer-Feedback boundary and persists one repair dispatch per call.                                                |
| `runValidationLoop offers another round or code review after three rounds`                                | repair       | removed     | Asserted prompt choice after three automatic semantic rounds in one driver loop; lifecycle refactor persists semantic round state and handles only one round per invocation.                                                        |
| `runValidationLoop continues to round four when the user asks for one`                                    | repair       | removed     | Asserted same-call fourth semantic round continuation; lifecycle validation no longer runs multiple semantic rounds inside one call.                                                                                                |
| `runValidationLoop halts without prompting when the repair baseline cannot be captured`                   | repair       | removed     | Asserted repair-baseline capture failure from old semantic repair setup; lifecycle refactor removed this capture-and-prompt sequence.                                                                                               |
| `runValidationLoop halts cleanly when the repair diff cannot be computed`                                 | repair       | removed     | Asserted repair diff computation failure in old continuation plumbing; lifecycle repair dispatch now receives the current diff for a single phase and does not compute retry diffs inside the driver.                               |
